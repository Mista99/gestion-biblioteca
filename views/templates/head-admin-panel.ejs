<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel de Usuario</title>
    <style>
        /* Variables globales */
        :root {
            --base-font-size: 62.5%;
            /* 1rem = 10px */
            --font-size-base: 1.6rem;
            /* 16px */
            --font-family: Arial, sans-serif;
            --bg-color-header: #333;
            --text-color-header: #fff;
            --bg-color-nav: #444;
            --text-color-nav: #fff;
            --padding-nav: 1rem;
            --border-radius: 0.5rem;
            --bg-color-odd-li: #e0e0e0;
            /* Color de fondo para los elementos impares */
        }

        /* Estilos generales */
        *,
        *:before,
        *:after {
            box-sizing: inherit;
            margin: 0;
            padding: 0;
        }

        body {
            box-sizing: border-box;
            font-family: var(--font-family);
            font-size: var(--font-size-base);
            line-height: 1.6;
            background-color: #f0f0f0;
            /* Ajuste para el header fijo */
            justify-content: center;
        }

        .menu {
            font-size: 1.2rem;
            list-style-type: none;
            display: flex;
            justify-content: center;
        }

        .menu li {
            margin-right: 1rem;
        }

        .menu li a {
            text-decoration: none;
            color: var(--text-color-nav);
            padding: 0.5rem 1rem;
            border-radius: var(--border-radius);
            transition: background-color 0.3s ease;
        }

        .menu li a:hover {
            background-color: rgba(255, 255, 255, 0.1);
        }

        @media (min-width: 1400px) {
            .menu {
                font-size: 1.6rem;
            }

            main {
                margin: 1rem auto;
                display: flex;
                width: 80%;
                flex-wrap: nowrap;
            }

            .content {
                background-color: #fff;
                padding: 2rem;
                margin: 0.5rem;
                border-radius: var(--border-radius);
                height: auto;
                flex: 1 1 calc(50% - 2rem);
            }
        }

        main {
            margin: 1rem auto;
            width: 90%;
        }

        .content {
            background-color: #fff;
            padding: 2rem;
            margin: 0.5rem;
            border-radius: var(--border-radius);
            height: auto;
        }

        .content h2 {
            font-size: 2rem;
            /* 20px */
            margin-bottom: 1rem;
        }

        .content p {
            font-size: 1.6rem;
            /* 16px */
            color: #555;
        }

        footer {
            text-align: center;
            padding: 1rem 0;
            background-color: var(--bg-color-header);
            color: var(--text-color-header);
        }

        footer p {
            margin: 0;
        }

        /* Estilos para formularios de gestión de libros y usuarios */
        .option form {
            background-color: #fff;
            padding: 1.5rem;
            border: 1px solid #ccc;
            border-radius: var(--border-radius);
            margin-bottom: 1rem;
        }

        .content-list {
            background-color: #fff;
            padding: 3rem;
            border: 1px solid #ccc;
            border-radius: var(--border-radius);
            margin-bottom: 1rem;
        }

        .content-list ul {
            list-style-type: none;
            padding: 0;
            /* Eliminar el padding predeterminado de ul */
        }

        .content-list li {
            padding: 0.5rem;
            /* Añadir 1rem de padding a cada caja de la lista */
            cursor: pointer;
            /* Cambiar el cursor a pointer al pasar el mouse */
        }
        .content-list input{
            padding: 0.5rem;
            /* Añadir 1rem de padding a cada caja de la lista */
            margin-top: 0.5rem;
            margin-bottom: 0.5rem;
        }
        .book-form input {
            padding: 1rem;
            /* Añadir 1rem de padding a cada caja de la lista */
            margin-top: 0.5rem;
            margin-bottom: 0.5rem;
        }

        /* Estilos para los elementos impares de .content-list */
        .content-list li:nth-child(odd) {
            background-color: var(--bg-color-odd-li);
        }

        .option form label {
            display: block;
            font-size: 1rem;
            /* 16px */
            margin-bottom: 0.5rem;
        }

        input {
            width: 100%;
            padding: 0.8rem;
            font-size: 1rem;
            /* 16px */
            margin-bottom: 1rem;
            border: 1px solid #ccc;
            border-radius: var(--border-radius);
        }

        .option form input,
        .option form select {
            width: 100%;
            padding: 0.5rem;
            font-size: 1rem;
            /* 16px */
            margin-bottom: 1rem;
            border: 1px solid #ccc;
            border-radius: var(--border-radius);
        }

        .option form button {
            background-color: #007bff;
            color: white;
            padding: 0.8rem 1.5rem;
            font-size: 1rem;
            /* 16px */
            border: none;
            border-radius: var(--border-radius);
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .book-details-container,
        .user-details-container {
            background-color: #f0f0f0;
            border: 1px solid #ccc;
            border-radius: 0.5rem;
            padding: 1rem;
            margin-top: 0.5rem;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            position: relative;
            /* Posición relativa para posicionar los detalles relativos al elemento clickeado */
            z-index: 1;
            /* Asegura que esté por encima del contenido normal */
        }

        .book-item,
        .user-item {
            cursor: pointer;
            /* Hace que el cursor sea un puntero al pasar sobre el elemento de lista */
            padding: 0.5rem;
            transition: background-color 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .book-item:hover,
        .user-item:hover {
            background-color: #f5f5f5;
            /* Color de fondo más claro al pasar el ratón sobre el elemento */
        }

        .book-details-container h2,
        .user-details-container h2 {
            font-size: 1.8rem;
            margin-bottom: 0.5rem;
        }

        .book-details-container ul,
        .user-details-container ul {
            list-style-type: none;
            padding: 0;
        }

        .book-details-container li,
        .user-details-container li {
            font-size: 1rem;
            color: #333;
            margin-bottom: 0.5rem;
        }

        /* Estilos delete */
        .details-item {
            cursor: pointer;
            padding: 0.5rem;
            transition: background-color 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .details-item:hover {
            background-color: #f8f9fa;
        }
    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">